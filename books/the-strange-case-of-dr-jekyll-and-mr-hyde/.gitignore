# This book is a stub: listed, but nobody has adopted it yet, so its text is not
# committed. A megabyte per book for novels nobody has started would bloat the
# repo for nothing.
#
# `bun run fetch-text <slug>` downloads it here when you start work.
#
# When you add your first waypoint, DELETE THIS FILE and commit source.txt. Every
# quote is checked against it, and CI needs it.
source.txt
candidates.json
.geocode-cache.json
